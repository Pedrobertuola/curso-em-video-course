title: Curso em video
description: Just simple programs.
template:
  repo: curso-em-video-template
  name: curso-em-video
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
