"""
Desafio 026
Problema: Faça um programa que leia uma frase pelo teclado
          e mostre quantas vezes aparece a letra "A", em que
          posição ela aparece a primeira vez e em que posição
          ela aparece a última vez.
Resolução do problema:
"""
F=str(input("Digite uma frase: ")).upper().strip()
print('A letra A aparece {} vezes'.format(F.count('A',0,)))
print('A primeira letra A aparece na posição {}'.format(F.find('A')+1))
print('A ultima letra A aparece na posição {}'.format(F.rfind('A')+1))
